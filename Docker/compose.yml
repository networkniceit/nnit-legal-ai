version: "3"

services:

  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      DATABASE_URL: postgres://postgres:pass@db:5432/legal

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: pass